spring:
  datasource:
    url: jdbc:mysql://localhost:3306/inventory-management
    username: 'root'
    password: '#Manishsdm28'
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update  # This will automatically update the schema (you can change it to 'none' or 'create' based on your need)
    show-sql: true  # This will print the SQL queries in the console for debugging
